# 🟦 **Python Basics (5 Questions)**

### **1. Print a Pattern**

Write a program to print the following pattern:

```
*
**
***
****
```

### **2. Simple Math Operations**

Given:

```python
a = 15
b = 4
```

Print the sum, difference, product, and quotient.

### **3. Swap Two Variables**

Swap the values of:

```python
x = 10
y = 5
```

**without using a third variable.**

### **4. Convert Celsius to Fahrenheit**

Given:

```python
c = 37
```

Convert to Fahrenheit using formula:
°F = (C × 9/5) + 32

### **5. String Formatting**

Given:

```python
name = "Anjali"
age = 20
```

Print:
`My name is Anjali and I am 20 years old.`
(using f-string)

---

# 🟦 **Flow Control (5 Questions)**

### **1. Check Positive, Negative, or Zero**

Given a number:

```python
num = -7
```

Print whether it is positive, negative, or zero.

### **2. Largest of Three Numbers**

Given:

```python
a = 12
b = 25
c = 9
```

Find the largest number using `if-elif-else`.

### **3. Print Even Numbers from 1 to 20**

Use a loop to print only even numbers.

### **4. Count How Many Numbers Are Greater Than 10**

Given:

```python
data = [3, 11, 7, 15, 22]
```

Count how many values are greater than 10.

### **5. Number Guess Loop**

Given:

```python
secret = 7
```

Write a loop that prints:

* “Too high” if guess > 7
* “Too low” if guess < 7
* “Correct” if equal
  (But **do not** use user input — use a fixed list of guesses)

---

# 🟦 **Data Structures (5 Questions)**

### **1. List Sum**

Given:

```python
numbers = [10, 20, 30, 40]
```

Calculate sum using a loop.

### **2. Dictionary Lookup**

Given:

```python
student = {"name": "Aditi", "age": 22, "score": 88}
```

Print each key and its value.

### **3. Remove Duplicates**

Given:

```python
items = [1, 2, 2, 3, 4, 4, 5]
```

Remove duplicates and print the set or unique list.

### **4. Count Occurrences**

Given:

```python
chars = ['a', 'b', 'a', 'c', 'a']
```

Count how many times `'a'` appears.

### **5. Merge Two Lists**

Given:

```python
a = [1, 3, 5]
b = [2, 4, 6]
```

Merge lists and sort them.

---

# 🟦 **Functions (5 Questions)**

### **1. Add Two Numbers**

Write a function `add(a, b)` that returns their sum.

### **2. Find Factorial**

Write a function `factorial(n)` using a loop.
Example: `factorial(5)` → `120`

### **3. Check Prime Number**

Write a function `is_prime(n)` that returns True/False.

### **4. Count Vowels**

Write a function that takes a string and returns the number of vowels.

### **5. Maximum of a List**

Write a function `max_list(lst)` that returns the largest number.

---

# 🟦 **Modules & Packages (5 Questions)**

### **1. Use the Math Module**

Import `math` and calculate:

* Square root of 81
* Value of π
* 5! (factorial)

### **2. Random Number Generator**

Use `random` module to print:

* A random integer from 1 to 100
* A random choice from list `[10, 20, 30]`

### **3. Current Date & Time**

Use `datetime` module to print:

* Current date
* Current time

### **4. Create Your Own Module**

Create a module named `mymath.py` containing:

* `add(a, b)`
* `subtract(a, b)`
  Then import and use it.

### **5. Operating System Info**

Using `os` module:

* Print current working directory
* List all files in the directory

---

If you want, I can also provide **solutions**, or convert these into a **PDF assignment sheet** for classroom distribution.
