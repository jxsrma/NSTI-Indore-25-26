**OOP practical questions**

### 1. Student Class

Create a class `Student` with:

* Attributes: name, roll_no, marks
* Method: `display_details()`

Create two student objects and display their details.

---

### 2. Bank Account

Create a class `BankAccount` with:

* Attribute: balance
* Methods:

  * `deposit(amount)`
  * `withdraw(amount)`
  * `show_balance()`

Demonstrate all three methods using an object.

---

### 3. Constructor Challenge

Create a class `Car` with:

* Attributes: brand, model, year
* Constructor to initialize values
* Method to display car details

Create at least two car objects.

---

### 4. Inheritance Practice

Create a class `Person` with:

* name
* age

Create a child class `Employee` that adds:

* salary

Display all details of an employee object.

---

### 5. Method Overriding

Create a class `Animal` with method:

```python
sound()
```

Create two child classes:

* Dog
* Cat

Override the `sound()` method in both classes and display different outputs.

---